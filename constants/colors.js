// ── 5 Approved Van Gogh App Colors ───────────────────────────────────────────
// Sourced from the right-side swatches of the color palette reference
// + the deep navy of Van Gogh's Starry Night painting.

/** Deep navy — from Starry Night. Primary text on light/canvas backgrounds. */
export const DEEP_NAVY    = '#1C2A44';

/** Warm vibrant orange — primary CTA, buttons, highlights. */
export const WARM_ORANGE  = '#E8522A';

/** Dark teal-slate — secondary surfaces, header elements, muted dark tone. */
export const SLATE_TEAL   = '#374950';

/** Warm tan-brown — secondary accent, muted text on light backgrounds. */
export const WARM_BROWN   = '#9B6846';

/** Soft terracotta-salmon — gentle accent, borders, warm subtle elements. */
export const SOFT_SALMON  = '#BE7A62';

// ── Utility colors (derived, not core palette) ────────────────────────────────
/** Pure white — card backgrounds, clean surfaces. */
export const CARD_WHITE   = '#FFFFFF';

/** Warm off-white — readable text on dark painting backgrounds. */
export const TEXT_LIGHT   = '#F7F0E8';

/** Muted warm grey — secondary text on dark painting backgrounds. */
export const MUTED_LIGHT  = '#C5B8A8';

// ── Semantic aliases (defined after dependencies) ─────────────────────────────
export const PRIMARY   = WARM_ORANGE;   // primary action / CTA
export const ACCENT    = WARM_BROWN;    // secondary accent
export const TEXT_DARK = DEEP_NAVY;     // dark text on light / canvas backgrounds
export const SURFACE   = SLATE_TEAL;    // dark surface (panels, headers)
export const CARD_BG   = CARD_WHITE;    // card background

/** Success green — semantic only, not part of the core 5. */
export const SUCCESS      = '#2D7A4A';

/** Error red — semantic only, not part of the core 5. */
export const ERROR        = '#C4503A';
